# Function Arguments
# --------------------------
# Functions can take arguments, which are passed on the stack
# in the order they are specified. The function can then access
# these arguments like any other variable.

function doStuff(foo: str, bar: str, baz: str): none
  foo puts
  bar puts
  baz puts
end

function main(): none
  "foo" "bar" "baz" doStuff
end
