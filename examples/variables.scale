#include "core.scale"

function main(): none
  // Create a new variable called 'foo'
  decl foo: str
  // push "Hello, World!" to the stack
  // and assign it to the variable 'foo'
  // this pops the string off the stack
  "Hello, world!" store foo
  // so we push it back onto the stack
  // and print it out
  foo puts
end