# a container is a static structure
container foo
  decl bar: str
  decl baz: [str]
end

function main(): none
  "load/store" => foo.bar
  foo.bar puts
  
  8 malloc => foo.baz
  "ref/deref" => @ foo.baz
  foo.baz @ puts
end
