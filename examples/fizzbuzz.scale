#include "core.scale"

function main(): none
  // FizzBuzz is a famous example of showing that
  // a programming language is turing complete
  1 while dup 100 < do
    dup 15 % 0 == if
      "FizzBuzz" puts
    else dup 3 % 0 == if
      "Fizz" puts
      else dup 5 % 0 == if
        "Buzz" puts
        else
          dup putint
        fi
      fi
    fi
    1 +
  done drop
end
